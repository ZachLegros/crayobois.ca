import React, { useState, useContext } from "react";
import "./userIcon.css";

const User = () => {
  return (
    <React.Fragment>
      <a href="/utilisateur"><i className="fas fa-user"></i></a>
    </React.Fragment>
  );
};

export default User;
