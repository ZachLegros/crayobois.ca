import React, { useState, useContext } from "react";

const Cart = props => {
  return (
    <React.Fragment>
        <span className="dashboard-content-header">Mon panier</span>
    </React.Fragment>
  );
};

export default Cart;
