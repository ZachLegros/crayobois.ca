import React, { useState, useContext } from "react";

const Settings = props => {
  return (
    <React.Fragment>
        <span className="dashboard-content-header">Paramètres</span>
    </React.Fragment>
  );
};

export default Settings;
